# Spatial 3D Simulation Case

This folder contains simulation results for the spatial tendon-driven continuum arm.

## Description

The spatial case considers a single 3D PCC segment controlled through the generalized coordinates:

```math
q = [\phi, \theta]^\top